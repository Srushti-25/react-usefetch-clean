export default [
  {
    rules: {
      // Warn if variables are declared but not used
      "no-unused-vars": "warn",

      // Allow console.log for debugging
      "no-console": "off",

      // Ensure consistent use of semicolons
      "semi": ["error", "always"],
    },
  },
];